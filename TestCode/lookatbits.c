#include <stdio.h>
#include <stdint.h>>

void printBinary();

int main(int argc, char *argv[]){
  
  //char c = 65;
  //int i = 65;
  //long int l = 65;
  //long long int m = 65;

  uint8_t e = 65;
  uint64_t s = 65;

  // sizeof() print outs bytes.
  //printf("%x\t%c\t%d\t%d\n",c, c, c, sizeof(c));
  //printf("%x\t%c\t%d\t%d\n",i, i, i, sizeof(i);
  //printf"%x\t%c\t%d\t%d\n",l, l, l, sizeof(l);

  printf"%x\t%c\t%d\t%d\n",m, m, m, sizeof(m);

  return 0;	
}
